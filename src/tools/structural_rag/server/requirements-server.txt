# FlexRIC RAG API server requirements
# Core (always required)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
requests>=2.31.0

# Optional — only needed for the llm_integration.py examples
# openai>=1.30.0
# anthropic>=0.28.0
# langchain>=0.2.0
