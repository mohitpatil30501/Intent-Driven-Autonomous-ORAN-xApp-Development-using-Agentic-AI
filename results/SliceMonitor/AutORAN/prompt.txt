SliceDetailMonitor a python 3 based xapp that help to monitor current information per slice. This help to see what slices are there. It needs simple logic, just get data and show/log it. xApp is subscription based, so every 10ms data is received from RIC. It's just monitoring so showing current info is enough as no action required. SliceDetailMonitor is a good name. I need information like Slice id, name, active UE count, type, etc.