Collect per-UE downlink and uplink throughput bytes from the MAC service model. Train a classifier to label each UE as high-load or low-load based on historical patterns. At runtime, use the model to decide whether to boost or reduce the UE's PRB allocation. Goal is smarter resource distribution.Use Python3 language. Historical data is - Each row is one UE observation with dl_aggr_tbs and ul_aggr_tbs as features and a binary high_load label."the live stream should exactly mirror the historical training data format. Same two columns, same units, no extras.